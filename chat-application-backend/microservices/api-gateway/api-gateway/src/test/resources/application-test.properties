spring.application.name=api-gateway-test
server.port=0

# Eureka standalone for tests
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Disable JWT validation in tests (use excluded paths)
jwt.secret=defaultSecretKeyThatIsLongEnoughForHs256Algorithm!!

# CORS
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedOriginPatterns[0]=http://localhost:5173
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedOriginPatterns[1]=http://localhost:3000
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedMethods[0]=GET
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedMethods[1]=POST
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedMethods[2]=PUT
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedMethods[3]=DELETE
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowedMethods[4]=OPTIONS
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowCredentials=true

# Actuator
management.endpoints.web.exposure.include=health,info,gateway
